[phases.setup]
cmds = [
    "source $(find . -maxdepth 1 -name '.env-pr-*')",
    "find . -maxdepth 1 -name '.env-pr-*' -exec sed -n '4p' {} +",
    "export $(find . -maxdepth 1 -name '.env-pr-*' -exec sed -n '4p' {} +)",
    "echo COOLIFY_BRANCH: $COOLIFY_BRANCH",
]
